<h1>Eat-Da-Burger</h1>
<div class="sub-container">
  <div class="left-side">
    <div class="header">Burgers that Need to be Eaten</div>

    <ul>
      {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=true}} {{/unless}} {{/each}}
    </ul>
  </div>

  <div class="right-side">
    <div class="header">Eaten Burgers</div>

    <ul>
      {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=false}} {{/if}} {{/each}}
    </ul>
  </div>
</div>
<div class="bottom-center">
  <h2>Place an Order Here</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="ca">Name of Burger:</label>
      <input type="text" id="ca" burger_name="burger_name">
    </div>

    <button type="submit">Add Burger to Order</button>
  </form>
</div>